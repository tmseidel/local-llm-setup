[llm_host]
inferencekoriander

[llm_host:vars]
ansible_user=tom
# SICHERHEIT: Klartext-Passwort nicht committen! Besser SSH-Keys verwenden
# oder per Ansible-Vault verschlüsseln (ansible-vault encrypt_string).
# Für sudo: --ask-become-pass beim Playbook-Aufruf nutzen.
ansible_password=tom
ansible_python_interpreter=/usr/bin/python3
